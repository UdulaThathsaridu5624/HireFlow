export enum UserRole {
  EMPLOYER = 'employer',
  CANDIDATE = 'candidate',
}

export enum JobStatus {
  OPEN = 'open',
  CLOSED = 'closed',
}

export enum ApplicationStatus {
  PENDING = 'pending',
  REVIEWED = 'reviewed',
  WITHDRAWN = 'withdrawn',
}
