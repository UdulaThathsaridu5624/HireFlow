info:
  name: Validate User
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/api/auth/validate
  auth:
    type: bearer
    token: "{{Bearer_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
