info:
  name: Get Pipelines (Candidate)
  type: http
  seq: 3

http:
  method: GET
  url: "{{interviewBaseUrl}}api/pipelines/candidate"
  headers:
    - name: Authorization
      value: "Bearer {{Bearer_token}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
