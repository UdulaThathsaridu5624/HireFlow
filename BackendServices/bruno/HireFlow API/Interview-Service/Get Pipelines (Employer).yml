info:
  name: Get Pipelines (Employer)
  type: http
  seq: 2

http:
  method: GET
  url: "{{interviewBaseUrl}}api/pipelines/employer"
  headers:
    - name: Authorization
      value: "Bearer {{Bearer_token}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
