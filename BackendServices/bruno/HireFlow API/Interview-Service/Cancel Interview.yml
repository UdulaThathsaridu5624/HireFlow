info:
  name: Cancel Interview
  type: http
  seq: 11

http:
  method: DELETE
  url: "{{interviewBaseUrl}}api/interviews/{{interviewId}}"
  headers:
    - name: Authorization
      value: "Bearer {{Bearer_token}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
