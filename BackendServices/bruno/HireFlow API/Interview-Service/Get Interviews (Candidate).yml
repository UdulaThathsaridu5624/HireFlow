info:
  name: Get Interviews (Candidate)
  type: http
  seq: 7

http:
  method: GET
  url: "{{interviewBaseUrl}}api/interviews/candidate"
  headers:
    - name: Authorization
      value: "Bearer {{Bearer_token}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
