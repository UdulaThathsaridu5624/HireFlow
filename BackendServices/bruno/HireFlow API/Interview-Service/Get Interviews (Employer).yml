info:
  name: Get Interviews (Employer)
  type: http
  seq: 6

http:
  method: GET
  url: "{{interviewBaseUrl}}api/interviews/employer"
  headers:
    - name: Authorization
      value: "Bearer {{Bearer_token}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
